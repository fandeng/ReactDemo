import React,{Component} from 'react';
import{
	StyleSheet,
	Text,
	View,
	BackAndroid,
	Platform,
	TouchableOpacity,
	Image,

	ScrollView,
} from 'react-native';
var ImagePicker = require('react-native-image-picker');
 // import ImagePickerManager from 'NativeModules';


import HeaderView from '../common/HeaderView';
import Common from '../common/common';


export default class MySetting extends Component{
	


	render(){

		return (
				<View >
				<HeaderView 
					titleView= {'安全设置'}
		            leftIcon={'angle-left'}
		            leftIconAction={() => this.props.navigator.pop() }
				/>
				<View style={{backgroundColor:'#D3D3D3',height:20,alignItems:'center',justifyContent: 'center'}}></View>
				<TouchableOpacity style={styles.view_style} onPress = {this.onClick.bind(this,1)}>
						<View style={{alignItems: 'flex-start',justifyContent: 'center',flex:7,marginLeft:20}}>
							<Text style={{}}>修改安全密码</Text>
						</View>
						<View style={{alignItems: 'flex-start',justifyContent: 'center',flex:1,marginLeft:20}}>
						<Image style={{height:30,width:15}} source={require('../img/listPromte.png')}></Image>
						</View>
				</TouchableOpacity>
				<View style={{height:1,backgroundColor:'#D3D3D3',alignItems:'center'}}></View>
				<TouchableOpacity style={styles.view_style} onPress = {this.onClick.bind(this,1)}>
					<View style={{alignItems: 'flex-start',justifyContent: 'center',flex:7,marginLeft:20}}>
							<Text style={{}}>修改手势密码</Text>
						</View>
						<View style={{alignItems: 'flex-start',justifyContent: 'center',flex:1,marginLeft:20}}>
						<Image style={{height:30,width:15}} source={require('../img/listPromte.png')}></Image>
						</View>
				</TouchableOpacity>

				</View>
			)
		}

	onClick(item){

	}
}

var styles =StyleSheet.create({
	view_style:{flex:1,justifyContent: 'flex-end',flexDirection:'row',height:60},
});
