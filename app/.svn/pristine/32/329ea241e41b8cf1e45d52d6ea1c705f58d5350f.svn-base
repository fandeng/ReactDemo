//个人信息界面
import React, { Component } from 'react';
import  {
  AppRegistry,
  StyleSheet,
  Text,
  View,
  ScrollView,
  Dimensions,
} from 'react-native';

import Common from '../common/common';
import HeaderView from '../common/HeaderView';

var MineInformationContainer = require('../components/MineInformation');

class MineInformationVC extends Component {
  render() {
    return (
      <View>
        <HeaderView  titleView= {'个人信息'}
        leftIcon={'angle-left'}
        leftIconAction={() => this.props.navigator.pop() }
        rightButton={'确认'}
        collectIconAction={() => this.props.navigator.pop()}/>
        {
          <ScrollView style={{height: Common.window.height-64,width:Common.window.width}}>
             <MineInformationContainer/>
          </ScrollView>
        }
      </View>
    );
  }
}


module.exports = MineInformationVC;
