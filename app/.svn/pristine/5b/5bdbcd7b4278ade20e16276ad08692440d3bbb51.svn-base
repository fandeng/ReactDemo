//协议订单
import React, { Component } from 'react';
import {
    StyleSheet,
    ScrollView,
    Text,
    Image,
    View,
    TouchableOpacity,
} from 'react-native';

import Common from '../common/common';
import HeaderView from '../common/HeaderView';

class ProtocolPage extends Component {
  render() {
      return(
         <View>
          <HeaderView  titleView= {'订单协议'}
          leftIcon={'angle-left'}
          leftIconAction={() => this.props.navigator.pop() }/>
          {
            <ScrollView style = {{width:Common.window.width,height:Common.window.height-64}}>
                <Image style={styles.image_style} source={require('../img/protocol.png')}/>
                <TouchableOpacity
                    activeOpacity={0.5}
                    onPress={()=>this.props.navigator.popToTop()}
                    style={styles.center}>
                    <View style = {styles.nextContainer_style}>
                        <Text style={{color:'white',fontSize:15}}>提交</Text>
                    </View>
                </TouchableOpacity>
                <View style={{height:30}}/>
            </ScrollView>
          }
         </View>
    );
  }
}
const styles = StyleSheet.create({
  nextContainer_style:{
    marginTop:20,
    marginLeft:10,
    marginRight:10,
    backgroundColor:'#1a98ff',
    height:40,
    borderRadius:5,
    justifyContent: 'center',
    alignItems: 'center',
  },
  image_style:{
    marginTop:10,
    width:Common.window.width,
    height:Common.window.height-64
  }
});
module.exports = ProtocolPage;
