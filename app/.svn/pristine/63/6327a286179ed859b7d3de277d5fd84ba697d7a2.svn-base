//我的贷款
'use strict'
import React, { Component } from 'react';
import {
    StyleSheet,
    Text,
    Image,
    ScrollView,
    View,
    Dimensions,
    Platform,
} from 'react-native';

import Common from '../common/common';
import HeaderView from '../common/HeaderView';
var MineLoanlist = require('../pages/MineLoanListView');

class MineLoanContainer extends Component {
    render() {
        return (
          <View>
            <HeaderView  titleView= {'我的贷款'}
            leftIcon={'angle-left'}
            leftIconAction={() => this.props.navigator.pop() }/>
            {
              <ScrollView style={{backgroundColor:'white', height: Common.window.height-64}}>
                  <MineLoanlist />
              </ScrollView>
            }
          </View>
      );
    }
}

module.exports = MineLoanContainer;
