import React, { Component } from 'react';
import {
    StyleSheet,
    Text,
    Image,
    ListView,
    View,
    Platform,
} from 'react-native';
import Common from '../common/common';

class MineLoanLV extends Component {
  	constructor(props) {
      	super(props);
      	this._renderRow = this.renderRow.bind(this);
      	this.state = {
        	 dataSource: new ListView.DataSource({
            	rowHasChanged: (row1, row2) => row1 !== row2,
        	 }),
        };
  	}
    render() {
      	let classList = ['不通过', '通过', '通过', '通过', '不通过'];
        return (
            <ListView
                dataSource={this.state.dataSource.cloneWithRows(classList) }
                renderRow={this._renderRow}
                enableEmptySections={true}
                style={{ height: Common.window.height-64}}//减去导航的高度
            />
       );
    }
  	renderRow(rowDate,sectionID: number | string, rowID: number | string,) {
      	return (
					<View style={{height:120}}>
          		<View style = {rowDate == '不通过'? styles.nopass_container:styles.passed_container}>
							    <View style={{alignItems:'center',flexDirection: 'row',height:20,marginTop:10,marginLeft:10}}>
									  <Text style = {styles.title_text}>金额（元）</Text>
										<Text style = {styles.title_text}>审核时间</Text>
									</View>
									<View>
									</View>

          		</View>
					</View>
      	);
  	}
		// <Text style = {rowDate == '不通过'?styles.nopass_title:styles.pass_title}>{rowDate}</Text>
}

const styles = StyleSheet.create({
			passed_container: {
				width: Common.window.width-20,
				height:90,
				marginLeft:10,
				marginTop:15,
				flexDirection: 'row',
				backgroundColor: 'rgba(198.0, 223.0, 247.0, 1.0)',
		  },
			nopass_container: {
				width: Common.window.width-20,
				height:90,
				marginLeft:10,
				marginTop:15,
				flexDirection: 'row',
				backgroundColor: 'rgba(201.0, 202.0, 203.0, 1.0)',
			},
      title_text: {
        textAlign: 'left',
        fontSize: 14,
        color: '#4d4d4d',
        marginTop:5,
        marginLeft:10,
      },
			pass_title:{
				textAlign: 'center',
        fontSize: 14,
        color: 'white',
        marginTop:60,
        marginLeft:Common.window.width/2-30,
				width:60,
				height:20,
				borderRadius:5,
				backgroundColor:'rgba(26.0, 130.0, 255.0, 1.0)'
			},
			nopass_title:{
				textAlign: 'center',
        fontSize: 14,
        color: 'white',
        marginTop:60,
        marginLeft:Common.window.width/2-30,
				width:60,
				height:20,
				backgroundColor:'red',
				borderRadius:5,
			},
});


module.exports = MineLoanLV;
